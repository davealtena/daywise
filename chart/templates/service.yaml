apiVersion: v1
kind: Service
metadata:
  name: {{ include "daywise.fullname" . }}
  labels:
    {{- include "daywise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daywise.selectorLabels" . | nindent 4 }}